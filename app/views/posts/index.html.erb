<div class = "posts">
  <div class = "into">
    <% @posts.each do |post| %>
      <p><%= post.score%>
        <span class="name"><%= post.subject %></span>
        <%# <%= link_to "削除", post_path(@post.ids), method: :delete, class: "postManage__delete" %> 
      </p>
    </div>
  <% end %>
  </div>
  <div class = "add">
    <%= link_to"情報を追加",new_post_path, method: :get %> 
  </div>

</div>
 